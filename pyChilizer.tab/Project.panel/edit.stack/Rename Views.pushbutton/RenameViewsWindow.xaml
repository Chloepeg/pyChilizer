<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Views"
        Height="600"
        Width="850"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <GroupBox Header="Find / Replace" Grid.Row="0" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Find:" VerticalAlignment="Center" />
                <TextBox x:Name="FindTextBox" Grid.Column="1" Margin="4,0" />

                <TextBlock Text="Replace:" Grid.Column="3" VerticalAlignment="Center" />
                <TextBox x:Name="ReplaceTextBox" Grid.Column="4" Margin="4,0" />

                <Button x:Name="FindReplaceButton"
                        Grid.Column="6"
                        Content="Apply"
                        Width="80"
                        Margin="4,0" />
                <Button x:Name="ResetButton"
                        Grid.Column="7"
                        Content="Reset"
                        Width="80"
                        Margin="4,0" />
            </Grid>
        </GroupBox>

        <DataGrid x:Name="ViewsGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  HeadersVisibility="Column"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Current Name"
                                    Binding="{Binding CurrentName}"
                                    IsReadOnly="True"
                                    Width="2*" />
                <DataGridTextColumn Header="New Name"
                                    Binding="{Binding NewName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Width="2*" />
                <DataGridTextColumn Header="View Type"
                                    Binding="{Binding ViewType}"
                                    IsReadOnly="True"
                                    Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,8,0,0">
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Width="90"
                    Margin="4,0" />
            <Button x:Name="OkButton"
                    Content="Apply"
                    Width="120"
                    Margin="4,0" />
        </StackPanel>
    </Grid>
</Window>

